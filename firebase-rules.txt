rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // 🎮 Treasure Hunt Game Rules - Allow all reads/writes for teams and submissions
    match /teams/{teamId} {
      allow read, write: if true;
    }
    
    match /submissions/{submissionId} {
      allow read, write: if true;
    }
    
    // Optional: Allow access to any document (most permissive for treasure hunt)
    match /{document=**} {
      allow read, write: if true;
    }
  }
} 